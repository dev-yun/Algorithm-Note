# HTTP 메서드의 GET POST PUT DELETE 중 GET을 이용해 특정 사이트 HTML소스코드 출력

import requests

target = 'https://google.com'   # 원하는 사이트 url 입력
response = requests.get(url=target)     # get - 특정 데이터의 생성을 요청
print(response.text)    # google html 소스를 받아와 출력함

'''input = 'https://google.com'
   output = <!doctype html><html itemscope="" itemtype="http://schema.org/WebPage" lang="en">....'''
