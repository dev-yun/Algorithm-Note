import itertools

data = [1,2,3]

for x in itertools.combinations(data, 2):   #조합(combinations)은 서로다른 data개수에서 순서를 고려하지 않고([1,2] = [2,1]이 된다.) r개를 뽑는 것.
  print(list(x), end=' ')                   # 수식으로 nCr = n! / r! * (n - r)!
  
  
'''input = [1, 2, 3]
   output = [1,2], [1,3], [2,3]'''
